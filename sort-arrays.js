// 7kyu Sort Arrays 1
// takes in an array as a parameter and asks us to return the array sorted in lexicographical order

// loop through each itme of the array and sort them in alphabetical order
const sortme = names => names.sort();


// Examples
console.log(sortme(['one', 'two', 'three' ]));
